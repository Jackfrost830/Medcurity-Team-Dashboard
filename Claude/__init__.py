"""Claude folder — Supabase CRM provider for the Team Dashboard.

See README.md for setup + rollback. This __init__.py just marks
the folder as a Python package so `import Claude.install_supabase_provider`
works on Vercel's serverless runtime (which doesn't auto-discover
namespace packages reliably).
"""
